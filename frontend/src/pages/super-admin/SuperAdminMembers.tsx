import React from 'react';
import Members from '../Members';

/** Global member view for super admin — reuses org member table & API */
const SuperAdminMembers: React.FC = () => <Members />;

export default SuperAdminMembers;
